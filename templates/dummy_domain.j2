server {
    listen 80;
    server_name {% for domain in le_domains %} {{ domain.names }} {% endfor %};

    location /.well-known/acme-challenge/ { alias {{ le_install_dir }}/.acme-challenges/; }

    location / {
         default_type text/plain;
         return 200 "scheme: $scheme";
    }
}

